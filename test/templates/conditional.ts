const base = 'hello';
/// return when(context.isReact, 'import React from "react";');
/// return when(context.isTypescript, 'const typed: string = "yes";');
/// return when(!context.isReact, 'const vanillaOnly = true;');
